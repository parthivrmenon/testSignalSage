apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: test
spec:
  selector:
    matchLabels:
      app: test
  endpoints:
  - port: test
    metricRelabelings:
    - sourceLabels: [__name__]
      regex: 'probe_http_duration_seconds'
      action: drop